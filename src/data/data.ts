import { Settings, Task } from "../utils/types/global-types";

/**
 * Array representing tasks
 */
export const tasks: Task[] = [];

/**
 * Settings of the bot
 */
export const settings: Settings = {
  taskChannelId: "1213565372700041337",
  lastMessageId: "",
};
